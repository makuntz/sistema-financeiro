# @pp-planning/ui-mobile

Componentes reutilizáveis para React Native / Expo.

Não compartilha componentes visuais com `@pp-planning/ui-web`.
Compartilha tokens, nomes e regras via `@pp-planning/design-tokens`.
