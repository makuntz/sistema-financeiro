# @pp-planning/api-client

Cliente tipado para consumo da API por web e mobile.

Suporta base URL, headers, token de autenticação (extensível), tratamento padronizado de erros e tipagem dos contratos.
